import React from 'react';
import ReactDOM from 'react-dom';

class Buttonneu extends React.Component {
    render() {
      return (
        <button>{this.props.name}</button>
      );
    }
  }

  export default Buttonneu;